FROM node:16-alpine
WORKDIR /app
COPY package.json .
RUN yarn
COPY . .

ENV DB_SERVER=<>
ENV DB_DATABASE=<>
ENV DB_USER=<>
ENV DB_PASSWORD=<>
ENV DB_TIMEOUT=600000

ENV PORT=4000
ENV LOCAL_SYSTEM_IP=*

EXPOSE 4000

CMD ["yarn", "start"]
